#include <iostream>

/// @brief Square root computation using the Newton-Raphson method.
///         This method solves the y = x^2 equation for x given y.
/// @return Square root of number
auto sqroot(double number) -> const double {
    double curr_der, curr_f;
    while (curr_tol > tol) {
        
    }
}
